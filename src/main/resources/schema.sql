CREATE TABLE IF NOT EXISTS Country (
    id INT AUTO_INCREMENT,
    name VARCHAR(250),
    capital VARCHAR(100),
    emoji VARCHAR(100),
    currency VARCHAR(50),
    code VARCHAR(50),
    PRIMARY KEY (id)
);